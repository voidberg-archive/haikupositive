{
  "name": "haikupositive",
  "version": "1.0.0",
  "description": "A simple module that returns random haikus.",
  "main": "src/index.js",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "babel --copy-files --out-dir dist src",
    "eslint": "eslint src",
    "test": "mocha src/index.test.js --compilers js:babel-register",
    "test:watch": "npm t -- -w",
    "cover": "nyc --reporter=lcov --reporter=text --require babel-register npm t",
    "check-cover": "nyc check-coverage --statements 100 --lines 100 --functions 100 --branches 100"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/voidberg/haikupositive.git"
  },
  "keywords": [
    "beos",
    "random",
    "haiku",
    "netpositive"
  ],
  "author": "Alexandru Badiu",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/voidberg/haikupositive/issues"
  },
  "homepage": "https://github.com/voidberg/haikupositive#readme",
  "dependencies": {
    "unique-random-array": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-2": "^6.5.0",
    "babel-register": "^6.9.0",
    "chai": "^3.5.0",
    "eslint": "^2.10.2",
    "eslint-config-airbnb": "^9.0.1",
    "eslint-plugin-import": "^1.8.0",
    "eslint-plugin-jsx-a11y": "^1.2.0",
    "eslint-plugin-react": "^5.1.1",
    "mocha": "^2.4.5",
    "nyc": "^6.4.4"
  },
  "babel": {
    "presets": [
      "es2015",
      "stage-2"
    ]
  },
  "config": {
    "ghooks": {
      "pre-push": "npm run eslint && npm run cover && npm run check-cover"
    }
  }
}
